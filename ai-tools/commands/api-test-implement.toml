name = "api-test-implement"
description = "Implement functional API tests based on a formal test plan"
prompt = """
Execute the following test implementation procedure:

<test_implementation_procedure>
@{ai-tools/instructions/test-implementation.md}
</test_implementation_procedure>

Target API and Feature:
'{{args}}'

NOTE: You MUST provide both an API name and a Feature name (e.g., 'School Search'). If '{{args}}' is incomplete, or if the strict discovery process below cannot find the target, STOP and ask me to clarify.

<discovery_rules>
@{ai-tools/instructions/shared-api-discovery.md}
</discovery_rules>
"""