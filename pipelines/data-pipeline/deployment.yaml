parameters:
  subscription: ''
  environmentPrefix: ''
  environment: ''
  cipEnvironment: ''
  buildNumber: ''
  workspaceDir: '$(Pipeline.Workspace)'
  importImage: true
  sourceEnvironmentPrefix: ''
  dependsOn: []

jobs:

  - deployment: DataPipelinePushImage
    displayName: 'Data Pipeline : Import Image'
    dependsOn: ${{ parameters.dependsOn }}
    condition: ${{ parameters.importImage }}
    pool:
      vmImage: ubuntu-latest
    environment: ${{ parameters.environment }}
    strategy:
      runOnce:
        deploy:
          steps:
            - task: AzureCLI@2
              inputs:
                azureSubscription: ${{ parameters.subscription }}
                inlineScript: |
                    az acr import --name myregistry --source ${{ parameters.sourceEnvironmentPrefix }}.azurecr.io/fbit-data-pipeline:${{ parameters.buildNumber }} --image fbit-data-pipeline:${{ parameters.buildNumber }}

  - deployment: DataPipelineProvision
    displayName: 'Data Pipeline : Provision'
    dependsOn: ${{ parameters.dependsOn }}
    pool:
      vmImage: ubuntu-latest
    environment: ${{ parameters.environment }}
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: none

            - download: current

            - template: ..\common\run-terraform.yaml
              parameters:
                subscription: ${{ parameters.subscription }}
                environmentPrefix: ${{ parameters.environmentPrefix }}
                environment: ${{ parameters.environment }}
                cipEnvironment: ${{ parameters.cipEnvironment }}
                module: 'data-pipeline'
                imageName: 'fbit-data-pipeline:${{ parameters.buildNumber }}'


  