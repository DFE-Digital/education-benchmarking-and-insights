parameters:
  subscription: ''
  environmentPrefix: ''
  environment: ''
  cipEnvironment: ''
  workspaceDir: '$(Pipeline.Workspace)'
  dependsOn: []

jobs:
  - deployment: ChartRendererDeployment
    displayName: 'ChartRenderer deployment'
    dependsOn: ${{ parameters.dependsOn }}
    pool:
      vmImage: ubuntu-latest
    environment: ${{ parameters.environment }}
    strategy:
      runOnce:
        deploy:
          steps:
            - checkout: none

            - download: current

            - template: ..\common\run-terraform.yaml
              parameters:
                subscription: ${{ parameters.subscription }}
                environmentPrefix: ${{ parameters.environmentPrefix }}
                environment: ${{ parameters.environment }}
                cipEnvironment: ${{ parameters.cipEnvironment }}
                module: 'chart-renderer'    

            - task: AzureCLI@2
              displayName: 'Whitelist Agent IP'
              inputs:
                azureSubscription: ${{ parameters.subscription }}
                scriptType: 'bash'
                scriptLocation: 'inlineScript'
                inlineScript: |
                  agentIP=$(curl -s https://api.ipify.org)
                  echo "Agent IP: $agentIP"
                  
                  storageName=$(az storage account list -g ${{ parameters.environmentPrefix }}-ebis-chart-renderer --query "[0].name" -o tsv)
                  appName="${{ parameters.environmentPrefix }}-ebis-chart-renderer-fa"
                  rgName="${{ parameters.environmentPrefix }}-ebis-chart-renderer"
                  
                  echo "Whitelisting $agentIP on Storage: $storageName"
                  az storage account network-rule add --account-name $storageName --resource-group $rgName --ip-address $agentIP
                  
                  echo "Whitelisting $agentIP on Function App: $appName"
                  az functionapp config access-restriction add --name $appName --resource-group $rgName --rule-name "PipelineAgent" --action Allow --ip-address $agentIP/32 --priority 100 --scm-site true
                  
                  # Small delay to allow DNS/Firewall propagation
                  sleep 30

            - task: AzureFunctionApp@2
              displayName: 'Deploy chart renderer function app'
              inputs:
                azureSubscription: ${{ parameters.subscription }}
                appType: functionAppLinux
                appName: '${{ parameters.environmentPrefix }}-ebis-chart-renderer-fa'
                isFlexConsumption: true
                package: '${{ parameters.workspaceDir }}/chart-renderer-apis/ChartRenderer.Api.zip'

            - task: AzureCLI@2
              displayName: 'Remove Whitelist'
              condition: always()
              inputs:
                azureSubscription: ${{ parameters.subscription }}
                scriptType: 'bash'
                scriptLocation: 'inlineScript'
                inlineScript: |
                  storageName=$(az storage account list -g ${{ parameters.environmentPrefix }}-ebis-chart-renderer --query "[0].name" -o tsv)
                  appName="${{ parameters.environmentPrefix }}-ebis-chart-renderer-fa"
                  rgName="${{ parameters.environmentPrefix }}-ebis-chart-renderer"
                  
                  echo "Removing $agentIP from Storage: $storageName"
                  az storage account network-rule remove --account-name $storageName --resource-group $rgName --ip-address $(curl -s https://api.ipify.org)
                  
                  echo "Removing $agentIP from Function App: $appName"
                  az functionapp config access-restriction remove --name $appName --resource-group $rgName --rule-name "PipelineAgent" --scm-site true
