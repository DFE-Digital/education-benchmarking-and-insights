parameters:
  workspaceDir: ''
steps:
  - checkout: none
  - download: current
  - task: ExtractFiles@1
    displayName: 'Extract e2e test files'
    inputs:
      archiveFilePatterns: '${{ parameters.workspaceDir }}/e2e-tests/e2e-tests.zip'
      destinationFolder: '${{ parameters.workspaceDir }}/e2e-tests-files'
  - task: qetza.replacetokens.replacetokens-task.replacetokens@6
    displayName: 'Replace tokens in appsettings.json'
    inputs:
      rootDirectory: '${{ parameters.workspaceDir }}/e2e-tests-files'
      targetFiles: '**/appsettings.json'
      escape: off
      tokenPrefix: '__'
      tokenSuffix: '__'
