@model Web.App.ViewModels.ResourceViewModel

@await Component.InvokeAsync("CommercialResource", new
{
    section = "Staff development and training",
    links = Array.Empty<(string, string)>(),
    displayHeading = Model.DisplayHeading
})