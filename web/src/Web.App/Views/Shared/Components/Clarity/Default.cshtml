@model Web.App.ViewModels.Components.ClarityViewModel


@if (!string.IsNullOrWhiteSpace(Model.ClarityProjectId) && Model.AnalyticsCookiesEnabled)
{
    <script type="text/javascript" add-nonce="true">
        (function(c,l,a,r,i,t,y){
            c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
            t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
            y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
            clarity('consentv2', {
                ad_Storage: "denied",
                analytics_Storage: "granted"
            });
        })(window, document, "clarity", "script", "@Model.ClarityProjectId");
    </script>
}
