@using Web.App.ViewModels.Components
@model Web.App.ViewModels.Components.LocalAuthoritySchoolFinancialViewModel

@foreach (var item in Model.SelectedOverallPhases)
{
    <input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.SelectedOverallPhases" value="@((int)item)"/>
}

@foreach (var item in Model.SelectedNurseryProvisions)
{
    <input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.SelectedNurseryProvisions" value="@((int)item)"/>
}

@foreach (var item in Model.SelectedSpecialProvisions)
{
    <input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.SelectedSpecialProvisions" value="@((int)item)"/>
}

@foreach (var item in Model.SelectedSixthFormProvisions)
{
    <input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.SelectedSixthFormProvisions" value="@((int)item)"/>
}

<input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.ResultAs" value="@((int)Model.ResultAs)"/>
<input type="hidden" name="@Model.FormPrefix@LocalAuthoritySchoolFinancialFormViewModel.FormFieldNames.FiltersVisible" value="hide"/>
