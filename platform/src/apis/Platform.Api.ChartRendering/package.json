{
  "name": "chart-rendering-fa",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "build-ci": "npm run prestart && copyfiles *.json out && copyfiles -u 1 \"dist/**/*\" out/dist && copyfiles -u 1 \"node_modules/**/*\" out/node_modules",
    "watch": "tsc -w",
    "clean": "rimraf dist && rimraf out",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "copyfiles": "^2.4.1",
    "rimraf": "^5.0.0",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/{index.js,functions/*.js}"
}